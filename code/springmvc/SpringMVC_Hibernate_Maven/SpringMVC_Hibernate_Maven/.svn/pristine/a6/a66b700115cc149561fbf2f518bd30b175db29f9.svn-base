//将隐藏域的值填入对应应该显示的位置
$(function(){
	$(".value").each(function(){
	
	var name=$(this).attr("name");
	var val=$("input[name="+name+"]").val();
	if(val!="undefined")
	{
		$(this).html(val);
	}
	else
	{
		alert("aaa");
	}
	
	})
})
